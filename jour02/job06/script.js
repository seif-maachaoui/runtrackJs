function konami (){
    
    //Je commence par écouter un évenement
    document.addEventListener('keydown', keyHandler, false);

    //Déclaration d'une variable
    let keyHandler = function (event){

    }

    //Je déclare une variable qui va contenir une combinaison de touches du clavier
    let cheat_code = ['ArrowUp', 'ArrowUp', 'ArrowDown', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'ArrowLeft', 'ArrowRight', 'b', 'a'];

    //Je déclare une variable qui va me permettre de compter le nb de touches appuyés
    let compteur = 0;






}

konami();